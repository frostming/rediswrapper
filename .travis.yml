language: python

python:
  - "2.7"
  - "3.3"
  - "3.4"
  - "3.5"

sudo: false

install:
  - pip install coveralls fakeredis redis

script: coverage run --source=rediswrapper setup.py test

after_success:
  coveralls
