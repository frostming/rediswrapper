language: python

python:
  - "2.7"
  - "3.3"
  - "3.4"
  - "3.5"

sudo: false

install:
  - pip install coveralls fakeredis redis pytest-cov

script: py.test --cov=rediswrapper

after_success:
  coveralls
